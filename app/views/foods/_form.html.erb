<%= form_tag(meals_path, remote: true) do |f| %>
  <%= hidden_field_tag :direction, params[:direction] %>
  <%= hidden_field_tag :sort, params[:sort] %>
  <%= select_tag :food, options_for_select(@food_names) %>
  <%= submit_tag "Add food" %>
<% end %>
